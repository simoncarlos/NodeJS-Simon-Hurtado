<!DOCTYPE html>
<html lang="es">
    <%- include('partials/head') %>
    <body>
        <h1><%- titulo %></h1>

        <% if (true) { %>
        <span><%= min %></span>
        <meter min="<%= min %>" max="<%= max %>" value="<%= nivel %>"></meter>
        <span><%= max %></span>
        <% } %> 
        
        <% numeros.forEach(num => { %>
        <div><%= num %></div>
        <% }); %>
    </body>
</html>