<!DOCTYPE html>
<html lang="es">
    <%- include('partials/head') %>
    <body>

        <% if (false) { %>
        <span><%= min %></span>
        <meter min="<%= min %>" max="<%= max %>" value="<%= nivel %>"></meter>
        <span><%= max %></span>
        <% } %> 
        
        <% numeros.forEach(num => { %>
        <div><%= num %></div>
        <% }); %>
    </body>
</html>